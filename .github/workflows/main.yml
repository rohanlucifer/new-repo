name: CI/CD. 
on:
  push:
    branches:
    - devops
  #pull_request:
   # branches:
    #- master

jobs:
  dev-branch:
         runs-on: ubuntu-latest
# Mobile folder job.
         steps:
             - uses: actions/checkout@v2
             - uses: dorny/paths-filter@v2
               id: bucket
               with:
                 filters: |
                   path:
                     - 'codes/**'
             - name: Setup Java
               if: steps.bucket.outputs.path == 'true'
               run: echo "hello-world"
             - uses: dorny/paths-filter@v2
               id: workflow
               with:
                 filters: |
                   workflow:
                     - 'rohan/**'
             - name: run the workflow
               if: steps.workflow.outputs.workflow == 'true'
               run: echo "hello from rohan"
